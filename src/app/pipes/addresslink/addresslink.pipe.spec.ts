import { AddresslinkPipe } from './addresslink.pipe';

describe('AddresslinkPipe', () => {
  it('create an instance', () => {
    const pipe = new AddresslinkPipe();
    expect(pipe).toBeTruthy();
  });
});
